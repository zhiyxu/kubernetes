apiVersion: v1
kind: Pod
metadata:
  generateName: pod-
spec:
  containers:
  - image: registry.k8s.io/pause:3.8
    name: pause
    ports:
    - containerPort: 80
    resources:
      limits:
        aliyun.com/gpu-mem: "4"
      requests:
        aliyun.com/gpu-mem: "4"
